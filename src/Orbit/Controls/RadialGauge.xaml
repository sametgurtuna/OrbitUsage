<UserControl x:Class="Orbit.Controls.RadialGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="Root">
    <Grid Width="{Binding Diameter, ElementName=Root}" Height="{Binding Diameter, ElementName=Root}">
        <Ellipse x:Name="TrackEllipse" Stroke="#26FFFFFF" />
        <Path x:Name="ArcPath" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="PercentText" Foreground="White" FontFamily="Segoe UI" FontWeight="SemiBold"
                       HorizontalAlignment="Center" />
            <TextBlock x:Name="LabelText" Foreground="#AAAAAA" FontFamily="Segoe UI"
                       HorizontalAlignment="Center" Margin="0,1,0,0" />
            <TextBlock x:Name="SubLabelText" Foreground="#888888" FontFamily="Segoe UI"
                       HorizontalAlignment="Center" Margin="0,1,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
